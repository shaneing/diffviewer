## 1. Implement CSS Alignment Updates

- [x] 1.1 Update `.line-num` in `src/styles.css` to include monospace font-family and explicit `line-height: 20px`
- [x] 1.2 Update `.line-code` in `src/styles.css` to include explicit `line-height: 20px` and reset `margin` to 0
- [x] 1.3 Update `.gutter-line-num` in `src/styles.css` to include explicit `line-height: 20px`
- [x] 1.4 Center `.gutter-line-num` horizontally and vertically using flexbox in `src/styles.css`

## 2. Verification

- [x] 2.1 Verify the app builds without errors
- [x] 2.2 Verify the vertical alignment and centering of code lines and line numbers in 1-way, 2-way, and 3-way views
